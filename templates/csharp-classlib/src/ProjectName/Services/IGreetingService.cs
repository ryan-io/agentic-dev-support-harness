namespace ProjectName.Services;

/// <summary>
/// Placeholder service proving the layout. Replace with real domain services;
/// keep the depend-on-abstractions rule (see code-standards: decoupling).
/// </summary>
public interface IGreetingService
{
    string GetGreeting();
}
