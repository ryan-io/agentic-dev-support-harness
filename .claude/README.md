# .claude/

Everything Claude Code reads at runtime. Two distinct subfolders, governed by different rules.

## rules/

The synced mirror of `../.github/instructions/`. Generated by `../.github/scripts/sync-claude-rules.py`, never edit by hand. The script transforms `applyTo` frontmatter into `paths` frontmatter and strips the `.instructions` suffix from the filename, because that's what Claude expects. The pre-commit hook regenerates this directory on every commit, so manual edits will be silently overwritten.

If you need to change a rule, edit the file in `../.github/instructions/` and let the sync handle the mirror.

## learning/

Data from the continuous-learning pipeline. `config.json` (thresholds and staleness windows) and `proposals/` (curated instruction-file change suggestions) are tracked. The rest (`observations.jsonl`, `instincts/`, `.session-notices/`, `session-delta.md`) is gitignored and lives only on the developer's machine.

See `../.github/scripts/learning/README.md` for the pipeline detail.

## sync_log.txt

Append-only log of every sync run. Gitignored. Useful when diagnosing why the mirror looks wrong.
