# Instructions

The canonical rule files. Both Copilot and Claude read from here, Copilot directly, Claude through synced copies in `.claude/rules/`. Edit only the files in this folder. The mirror is regenerated by `../scripts/sync-claude-rules.py` on every commit.

## Frontmatter

Every file declares its scope with `applyTo: "<glob>"`. The sync script transforms this into `paths: ["<glob>"]` when writing the Claude mirror. Instruction files must stay at or below 4,000 characters. Files with no frontmatter or an empty body are rejected by the validation workflow. Files prefixed with `<!-- DEPRECATED` are skipped by the sync script.

## What's here

Core universal rules apply to all files: `code-standards`, `writing-voice`, `research`, `agent-guardrails`, `pattern-fidelity`, `memory`. Extended rules are scoped by file type: `testing` and `pr-review` to code files, `user-interface` and `user-experience` to UI files. Domain rules attach to specific directories: `adr-template` and `adr-pr-review` to `docs/adr/**`, `br-review` to `docs/business-rules/**`. Stack-specific code standards (`csharp-code-standards`, `lua-code-standards`, `unity-code-standards`) extend the universal one; `unity-code-standards` also supersedes named C# rules within `Assets/**`. `patterns` is a deprecated placeholder, not synced until patterns are documented; run the `convention-discovery` skill to mine the first entries from git history.

## When to add a file

Add a new instruction file when a rule applies to a directory or file type that none of the existing files cover. Keep agnostic content out of stack-specific files and stack content out of agnostic ones. If the rule is a one-off architectural decision, write an ADR instead.

## Related

- [Skills](../skills/README.md): on-demand procedures, including the ones that author files in scoped directories.
- [Reference docs](../docs/README.md): the templates and companion guides these thin rules point to.
- [Scripts](../scripts/README.md): the sync that regenerates the Claude mirror from these files.
- [`.claude/`](../../.claude/README.md): where the synced mirror lives.
- [Repo map](../../README.md): the high-level overview of every system.
